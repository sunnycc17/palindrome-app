import "./App.css";

const App: React.FC = () => {
  return (
    <div className="container text-center mt-5">
      <h1 className="text-primary">Hello, Bootstrap + React + TypeScript!</h1>
      <button className="btn btn-success">Click Me</button>
    </div>
  );
};

export default App;
